// Signing.xcconfig
//
// Copy this file to Signing.xcconfig (same directory) and set your
// Apple Developer Team ID below. Signing.xcconfig is gitignored so
// each contributor keeps their own team on their machine without
// committing it.
//
// To find your team ID:
//   Xcode > Settings > Accounts > (your Apple ID) > Manage Certificates
//   Or: developer.apple.com/account > Membership details
//   A free personal Apple ID also works for on-device development.
//
// Optional: override PRODUCT_BUNDLE_IDENTIFIER here when your personal
// team cannot register the shipping bundle ID (use a *.dev suffix).
//
// Uncomment and fill in:
// DEVELOPMENT_TEAM = YOUR10CHARID
// PRODUCT_BUNDLE_IDENTIFIER = sh.mateo.empo.dev

// Optional: point at the entitlements file with no iCloud keys when
// your team cannot register the container `iCloud.sh.mateo.empo`.
// The default is Empo.entitlements, which holds them. With this
// override the iCloud target hides itself at runtime, per SPEC 9.1.
//
// EMPO_ENTITLEMENTS = Empo-NoICloud.entitlements
