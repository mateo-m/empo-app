diff --git a/configure.ac b/configure.ac
index 28fdfefbbc..796d349ca2 100644
--- a/configure.ac
+++ b/configure.ac
@@ -2847,7 +2847,7 @@ AS_IF([test "$with_dln_a_out" != yes], [
 			: ${DLDSHARED='$(CC) -dynamiclib'}
 			: ${LDFLAGS=""}
 			: ${LIBPATHENV=DYLD_FALLBACK_LIBRARY_PATH}
-			: ${PRELOADENV=DYLD_INSERT_LIBRARIES}
+
                         AS_IF([test x"$enable_shared" = xyes], [
                             # Resolve symbols from libruby.dylib when --enable-shared
                             EXTDLDFLAGS='$(LIBRUBYARG_SHARED)'
diff --git a/dir.c b/dir.c
index c19ef637a1..355c9deacd 100644
--- a/dir.c
+++ b/dir.c
@@ -97,7 +97,27 @@ char *strchr(char*,char);
 # define NORMALIZE_UTF8PATH 1
 # include <sys/param.h>
 # include <sys/mount.h>
-# include <sys/vnode.h>
+# include <TargetConditionals.h>
+# if TARGET_OS_IPHONE
+/* sys/vnode.h is not available in the iOS SDK; define the needed constants */
+#  ifndef VREG
+#   define VREG 1
+#  endif
+#  ifndef VDIR
+#   define VDIR 2
+#  endif
+#  ifndef VLNK
+#   define VLNK 5
+#  endif
+#  ifndef VT_HFS
+#   define VT_HFS 17
+#  endif
+#  ifndef VT_CIFS
+#   define VT_CIFS 23
+#  endif
+# else
+#  include <sys/vnode.h>
+# endif
 #else
 # define NORMALIZE_UTF8PATH 0
 #endif
diff --git a/process.c b/process.c
index 674f05dd6c..512ad8584c 100644
--- a/process.c
+++ b/process.c
@@ -13,6 +13,10 @@
 
 #include "ruby/internal/config.h"
 
+#ifdef __APPLE__
+# include <TargetConditionals.h>
+#endif
+
 #include "internal/scheduler.h"
 #include "coroutine/Stack.h"
 
@@ -4604,7 +4608,11 @@ rb_spawn_process(struct rb_execarg *eargp, char *errmsg, size_t errmsg_buflen)
         rb_last_status_set(0x7f << 8, pid);
     }
 # else
+#if TARGET_OS_IPHONE
+    status = -1; /* system() is unavailable on iOS */
+#else
     status = system(rb_execarg_commandline(eargp, &prog));
+#endif
     pid = 1;			/* dummy */
     rb_last_status_set((status & 0xff) << 8, pid);
 # endif
