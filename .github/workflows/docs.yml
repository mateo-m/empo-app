name: Docs

on:
  pull_request:
    paths:
      - "docs/**"
      - "landing/**"
      - "pages/**"
      - "public/**"
      - "theme.css"
      - "blume.config.ts"
      - "package.json"
      - "bun.lock"
      - ".github/workflows/docs.yml"
  push:
    branches: [main]
    paths:
      - "docs/**"
      - "landing/**"
      - "pages/**"
      - "public/**"
      - "theme.css"
      - "blume.config.ts"
      - "package.json"
      - "bun.lock"
      - ".github/workflows/docs.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    name: Build
    runs-on: ubuntu-latest
    steps:
      # The changelog page reads the engine commits between the submodule
      # pins of two release tags, so it needs the tags and the engine history.
      - uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Fetch the engine history
        run: git submodule update --init mkxp-z-apple-mobile

      - uses: oven-sh/setup-bun@v2

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Build the site
        run: bun run docs:build

      - name: Upload the Pages artifact
        if: github.event_name != 'pull_request'
        uses: actions/upload-pages-artifact@v5
        with:
          path: dist

  deploy:
    name: Deploy
    if: github.event_name != 'pull_request'
    needs: build
    runs-on: ubuntu-latest
    permissions:
      pages: write
      id-token: write
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - name: Deploy to GitHub Pages
        id: deployment
        uses: actions/deploy-pages@v5
