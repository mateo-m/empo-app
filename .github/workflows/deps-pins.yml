name: Deps pins

on:
  pull_request:
  push:
    branches: [main]
    tags:
      - "v*.*.*"

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
  empo-deps:
    name: Verify empo-deps release pins
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
        with:
          submodules: false

      - name: Install gh CLI
        run: |
          type -p curl >/dev/null || sudo apt install curl -y
          curl -fsSL https://cli.github.com/packages/githubcli-archive-keyring.gpg \
            | sudo dd of=/usr/share/keyrings/githubcli-archive-keyring.gpg
          sudo chmod go+r /usr/share/keyrings/githubcli-archive-keyring.gpg
          echo "deb [arch=$(dpkg --print-architecture) signed-by=/usr/share/keyrings/githubcli-archive-keyring.gpg] https://cli.github.com/packages stable main" \
            | sudo tee /etc/apt/sources.list.d/github-cli.list >/dev/null
          sudo apt update
          sudo apt install gh -y

      - name: Verify pins against empo-deps
        env:
          GH_TOKEN: ${{ secrets.EMPO_DEPS_READ_TOKEN || github.token }}
          REQUIRE_PUBLISHED: ${{ startsWith(github.ref, 'refs/tags/v') && '1' || '0' }}
        run: scripts/verify-empo-deps-pins.sh
